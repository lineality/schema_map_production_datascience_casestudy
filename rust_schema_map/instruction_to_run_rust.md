# To run code:

1. Install Rust: https://rustup.rs/ is the official Rust installer

2. Go to (or create) directory containing /src directory, cargo.toml, and the schema txt files

2. In terminal
```bash
cargo run
```
